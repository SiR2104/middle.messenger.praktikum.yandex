import SearchInput from 'bundle-text:./index.flopa';
import Template from '../../utils/Template';
import '../ChatCard/index.scss';

export default (params, attributes) => new Template(SearchInput,null, params, attributes);
