import ChatTopPanel from 'bundle-text:./index.flopa';
import Template from '../../utils/Template';
import '../ChatTopPanel/index.scss';

export default (params, attributes) => new Template(ChatTopPanel,null, params, attributes);
